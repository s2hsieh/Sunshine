export enum Feature{
    now = "conditions",
    three = "forecast",
    ten = "forecast10day"
}

export enum Distance{
    metric = "km",
    imperial = "mi"
}

export enum Speed{
    metric = "km/h",
    imperial = "mi/h"
}

export enum Pressure{
    metric = "mb",
    imperial = "in"
}

export enum Volume{
    metric = "mm",
    imperial = "in"
}

export enum Degree{
    metric = "c",
    imperial = "f"
}

export enum Observation{
    temp,
    wind,
    precip,
    pressure,
    visibility
}